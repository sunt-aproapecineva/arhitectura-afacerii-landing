# dependencies
node_modules

# build output
dist
dist-ssr

# local env & secrets
.env
.env.*
*.local
*.pem
*.key

# editor / OS
.DS_Store
.vscode
.idea
.claude

# arhive & referințe (nu fac parte din app)
_archive/*
# excepție: originalele imaginilor de etape, înainte de re-encodare la 1400x800.
# Fără ele nu se mai pot regenera la altă rezoluție. (Trebuie `_archive/*`, nu
# `_archive` — git nu poate re-include un fișier dintr-un director deja exclus.)
!_archive/etape-orig/
design-system

# logs
*.log
npm-debug.log*
